spring :
  datasource:
    url: jdbc:mysql://localhost:3306/serverdb
    username: root
    password : Raed+rym2
  jpa :
    hibernate:
      ddl-auto: update
    properties:
          hibernate:
          dialect: org.hibernate.dialect.MySQLDialect
          format_sql: true
          show-sql : true
